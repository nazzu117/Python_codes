# import logging, platform


# class HostnameFilter(logging. Filter):
#     hostname = platform.node()
#     def filter(self, record):
#         record.hostname = HostnameFilter.hostname
#         return True


# start_time = time.time()
# filename = datetime.now().strftime("logs_d_m_%Y.txt")
# logging.basicConfig(filename filename, level-logging.INFO)
# logger logging.getLogger("Testjob")
# logging.info("Program started ....")
# logger.setLevel (logging.INFO)
# log_stringio io.String10()
# handler logging.StreamHandler (log_stringio)
# handler.addFilter (HostnameFilter())
# handler.setFormatter (logging.Formatter (%(asctime)s %(levelname)s %(hostname)s %(name)s: %(message)s', datefmt-%d-%b-xy
# logger.addHandler (handler)
# logging.info("List has all objects from $3... good")
# logging.info("All created lists are populated with elements from S3... good") logging.info("Dictionary and Dataframe has been created... good")
# logging.info("Convert dataframe to csv... good")